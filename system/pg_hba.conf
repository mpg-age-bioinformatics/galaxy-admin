# /var/lib/pgsql/data/pg_hba.conf

# TYPE  DATABASE        USER              ADDRESS                 METHOD
local   all             %%GA_SQLUSER%%                            trust
local   all             postgres                                  trust
host    all             all               127.0.0.1/32            trust
host    all             all               ::1/128                 ident

